# Sinced - 개발 계획

## 개발 단계

### Phase 1: 프로젝트 초기화
- [ ] Android 프로젝트 골격 생성 (Kotlin + Jetpack Compose)
- [ ] Gradle 설정 (Compose BOM, Min SDK 26, Target SDK 34, 패키지 `com.sinced`)
- [ ] Material3 테마 적용 (시스템 테마 따름)
- [ ] Git 저장소 초기화 + GitHub 연동 (`docs/00_환경_설정.md` 참고)
- [ ] `.gitignore` 설정 (Android 표준)

### Phase 2: 데이터 계층
- [ ] Room DB 설정 (`SincedDatabase`, schema version 1)
- [ ] 엔티티: `Category`, `Item`, `LogEntry`
- [ ] DAO: `CategoryDao`, `ItemDao`, `LogEntryDao`
  - 항목 + 마지막 기록 시각 조인 쿼리 (`ItemWithLastLog`)
  - Flow 기반 observe
- [ ] Repository 계층 (DAO를 추상화)
- [ ] 첫 실행 시 기본 카테고리 시드

### Phase 3: 도메인 로직
- [ ] 파생 값 계산 (`daysSince`, `daysUntilDue`, `status`)
- [ ] 정렬/필터링 유틸 (메모리 내 처리)
- [ ] 날짜 유틸 (epoch millis ↔ LocalDate, 일수 차이 계산 — 자정 기준)

### Phase 4: 메인 화면
- [ ] `MainScreen` 구성: 필터 칩 + 정렬 + 항목 리스트
- [ ] 항목 카드: 상태별 색상, 경과일/D-day 표시
- [ ] 빠른 기록 (롱프레스 / 스와이프)
- [ ] 보관(archive) 스와이프 + 스낵바 "되돌리기"
- [ ] FAB → 항목 추가
- [ ] 필터/정렬 상태 SharedPreferences 저장

### Phase 5: 항목 추가/편집
- [ ] `ItemEditScreen` (신규/편집 공용)
- [ ] 카테고리 선택 칩 + "+ 추가"
- [ ] 주기 설정 토글
- [ ] "첫 기록도 함께 등록" 옵션 (소급 입력)
- [ ] 입력 검증 (이름 필수, 주기 양수)

### Phase 6: 항목 상세 / 이력
- [ ] `ItemDetailScreen`: 상태 요약 + 이력 리스트
- [ ] 기록 추가 다이얼로그 (DatePicker, 빠른 선택 칩)
- [ ] 기록 수정/삭제
- [ ] 항목 보관/영구삭제

### Phase 7: 카테고리 관리
- [ ] `CategoryManageScreen`
- [ ] 추가/이름변경/색상/순서변경/삭제
- [ ] 삭제 시 영향받는 항목 수 안내

### Phase 8: 마무리
- [ ] 빈 상태 UI (항목 0개 / 필터 결과 0개)
- [ ] 기본 색상 팔레트 확정
- [ ] 빌드 검증 (`./gradlew assembleDebug`)
- [ ] 실기기 동작 확인 (USB 연결 / `adb install`)
- [ ] GitHub 푸시

### 후순위 (1차 버전 외)
- [ ] 알림 (WorkManager + 알림 권한)
- [ ] 위젯 (홈 스크린에 임박 항목)
- [ ] JSON 내보내기/가져오기
- [ ] Google Drive 백업/동기화
- [ ] 위젯, 다국어
- [ ] 평균 주기 자동 계산/제안

## 프로젝트 구조 (예정)

```
app/src/main/java/com/sinced/
├── MainActivity.kt
├── SincedApplication.kt
├── data/
│   ├── SincedDatabase.kt
│   ├── entity/
│   │   ├── Category.kt
│   │   ├── Item.kt
│   │   └── LogEntry.kt
│   ├── dao/
│   │   ├── CategoryDao.kt
│   │   ├── ItemDao.kt
│   │   └── LogEntryDao.kt
│   ├── relation/
│   │   └── ItemWithLastLog.kt
│   ├── repository/
│   │   ├── CategoryRepository.kt
│   │   ├── ItemRepository.kt
│   │   └── LogRepository.kt
│   └── seed/
│       └── DefaultCategories.kt
├── domain/
│   ├── model/
│   │   ├── ItemStatus.kt
│   │   └── ItemView.kt          # UI에 표시되는 파생 값 포함
│   └── util/
│       ├── DateUtil.kt
│       └── ItemDeriver.kt        # daysSince, daysUntilDue, status 계산
├── ui/
│   ├── theme/
│   │   ├── Color.kt
│   │   ├── Theme.kt
│   │   └── Type.kt
│   ├── nav/
│   │   └── SincedNavGraph.kt
│   ├── main/
│   │   ├── MainScreen.kt
│   │   ├── MainViewModel.kt
│   │   └── components/
│   │       ├── ItemCard.kt
│   │       ├── FilterBar.kt
│   │       └── SortMenu.kt
│   ├── itemedit/
│   │   ├── ItemEditScreen.kt
│   │   └── ItemEditViewModel.kt
│   ├── itemdetail/
│   │   ├── ItemDetailScreen.kt
│   │   ├── ItemDetailViewModel.kt
│   │   └── components/
│   │       └── LogRow.kt
│   ├── log/
│   │   └── LogEntryDialog.kt
│   ├── category/
│   │   ├── CategoryManageScreen.kt
│   │   └── CategoryViewModel.kt
│   ├── settings/
│   │   └── SettingsScreen.kt
│   └── common/
│       └── SincedViewModelFactory.kt   # Application에서 만든 Repository를 ViewModel에 주입
└── prefs/
    └── UserPreferences.kt              # 정렬/필터 저장
```

> DI 프레임워크 없음 — `SincedApplication`이 DB/Repository를 만들어 보유하고,
> 공용 `SincedViewModelFactory`가 화면별 ViewModel에 필요한 Repository를 주입.
> Hilt는 화면 수가 더 늘면 도입 검토 (FitLog는 Hilt 사용, Muffle은 미사용 — Sinced는 Muffle 쪽 가벼운 패턴 채택).

## 의존성 (확정 — FitLog/Muffle과 동일 라인업)

플러그인 / 컴파일러:
- AGP `8.2.2`, Kotlin `1.9.22`, Compose Compiler Extension `1.5.8`, JVM Target 17

```kotlin
// build.gradle.kts (app)
dependencies {
    // Core
    implementation("androidx.core:core-ktx:1.12.0")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.7.0")

    // Compose
    implementation(platform("androidx.compose:compose-bom:2024.02.00"))
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.ui:ui-graphics")
    implementation("androidx.compose.ui:ui-tooling-preview")
    implementation("androidx.compose.material3:material3")
    implementation("androidx.compose.material:material-icons-extended")
    implementation("androidx.activity:activity-compose:1.8.2")

    // Lifecycle / ViewModel
    implementation("androidx.lifecycle:lifecycle-viewmodel-compose:2.7.0")
    implementation("androidx.lifecycle:lifecycle-runtime-compose:2.7.0")

    // Navigation
    implementation("androidx.navigation:navigation-compose:2.7.7")

    // Room (FitLog와 동일하게 kapt 사용 — Muffle은 Room 미사용)
    implementation("androidx.room:room-runtime:2.6.1")
    implementation("androidx.room:room-ktx:2.6.1")
    kapt("androidx.room:room-compiler:2.6.1")

    // Coroutines
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.7.3")

    // Test
    testImplementation("junit:junit:4.13.2")
    debugImplementation("androidx.compose.ui:ui-tooling")
    debugImplementation("androidx.compose.ui:ui-test-manifest")
}
```

> Compose BOM `2024.02.00`은 FitLog/Muffle 동일 환경에 맞춘 결정.
> 신규 Material3 컴포넌트가 필요해지면 그때 BOM 업데이트.

## 테스트 정책 (1차 — 가벼운 검증)
- 핵심 도메인 유틸(`ItemDeriver`, 날짜 계산)에 대한 단위 테스트만 작성
- UI 테스트는 후순위
